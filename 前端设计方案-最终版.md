# STAR WARS Zero Company 攻略站 — 前端设计方案【最终版】

> v2.0 FINAL ｜ 2026-08-30 ｜ 方向：**A（HUD 战术终端）主干 + B（星战电影）首屏** ｜ 交付：Codex 复刻 ｜ 参考效果图：mockups/（index / performance / classes / worth-it，四页互通，浏览器直接打开）

---

## 0. 定稿方向

- **骨架 = A**：深空蓝黑底 + cyan HUD 功能色 + 斜切角卡片 + 高密度工具型内容区。保证攻略站的检索效率。
- **首屏 = B**：首页 Hero 为**全幅 key art 底图 + 星战金 Orbitron 大标题 + 琥珀 kicker 引言**，解决"第一眼"与社媒截图传播。
- **子页面**用与首页同源的 `.page-hero` 页头带（金色 kicker + 大标题 + mono 元信息），全站视觉统一。
- 金色仅出现在：Logo、首页 Hero 主标题、页头 kicker 装饰符（◈）。正文功能色仍是 cyan/amber，克制不泛滥。

---

## 1. Design Tokens（写入 tailwind.config + CSS variables）

```css
--bg-primary:   #0A0E17   /* 页面底 */
--bg-surface:   #111827   /* 卡片 */
--bg-elevated:  #1A2333   /* 悬浮 */
--accent-cyan:  #22D3EE   /* 功能主色：链接/图标/数据/边框 */
--accent-amber: #F59E0B   /* 强调：CTA、热点、警告、kicker */
--accent-gold:  #FFE81F   /* 品牌金：仅 Logo/Hero 标题/kicker 装饰 */
--text-primary: #E5EAF2
--text-muted:   #8B98AD
--success: #34D399  --danger: #F87171
--tier-s/a/b/c/d: #F59E0B / #22D3EE / #34D399 / #8B98AD / #64748B
--border: 1px solid rgba(34,211,238,.15)
```

**字体**（next/font 加载）：
- `Orbitron`（700/900）— 标题、Logo、按钮、数字大字
- `Inter`（400/500/600）— 正文 UI
- `JetBrains Mono` — 数据、配置、kicker、日期线
- 标题装饰规则：kicker = mono + `letter-spacing:.3em` + 全大写 + amber

**装饰体系**：
1. 斜切角卡片：`clip-path: polygon(12px 0,100% 0,100% calc(100%-12px),calc(100%-12px) 100%,0 100%,0 12px)`，仅用于 Hub 卡/内容卡，按钮用 10px 版
2. HUD 分区标签：`// SECTION NAME`（Orbitron 11px + .3em 字距，`//` 用 amber）
3. 战术网格纹理：repeating-linear-gradient 47px 间隔，cyan 3-5% 透明度，仅 Hero 底部衔接区与 Wiki 页头
4. 胶片扫描线：Hero 全幅叠加 `repeating-linear-gradient(0deg, transparent 0 3px, rgba(0,0,0,.18) 3px 4px)`

---

## 2. 首页 Hero（最终融合版，核心复刻对象）

结构（见 mockups/index.html）：
1. **底层**：全幅 key art 位（小队黄昏剪影，琥珀主光 + cyan 边缘光；素材未到位前用径向渐变占位，禁止放官方原图）
2. **叠加**：三层渐变（顶部透明 → 底部融入 bg-primary）+ 琥珀/青色双 radial 氛围光 + 扫描线 + 底部战术网格
3. **内容**（左对齐，底部锚定，min-height 82vh）：
   - kicker（amber mono）：`It is a dark time for the Republic — mercenaries answer the call`
   - 主标题（Orbitron 900, **#FFE81F**, 58px, 辉光 text-shadow）：`ZERO COMPANY` + 副行（20px, .28em 字距, 白色）
   - 一句定位文案（≤ 90 字符）
   - 双 CTA：金色实心 `FIX YOUR FPS FIRST`（ amber 亦可，二选一保持唯一主 CTA）+ cyan 描边 `BEST CLASSES →`
4. Hero 上方保留 **amber 热点数据条**（73K 峰值 / 好评率回升 / +40 FPS），mono 字体，数据型信任钩子

## 3. 子页面页头 `.page-hero`

- 背景：bg-primary + 琥珀（右上）/ cyan（左上）低透明 radial
- 结构：`◈ kicker（金色装饰符 + amber mono）` → H1（Orbitron 36px，关键词用 cyan `<em>`）→ mono 元信息行（更新日期 · 验证版本 · 平台）
- 面包屑置于页头之上，全站二级页必配

---

## 4. 全站页面结构（对照网站架构初版）

### 4.1 全局
- Header：sticky + 毛玻璃，Logo（金 + cyan 分段）+ 7 项导航 + Cmd+K 搜索（移动端汉堡）
- Footer：4 列站点地图 + **IP 免责声明**（必带：Not affiliated with EA/Bit Reactor/Lucasfilm/Disney）
- 每篇内容页开头：`Mission Briefing` TL;DR 卡（cyan 左边框，amber 加粗关键词）— 答案前置
- 内容页右侧 sticky TOC + "Related Intel" 内链卡（移动端隐藏 TOC）

### 4.2 首页（mockups/index.html）
热点条 → Hero（融合版）→ 事实速览 6 网格 → 新手三连卡（Fix FPS / Best Classes / Beginner Guide）→ 六分区 Hub 卡（2×3，每卡含 3 条最新链接）→ 角色 Dossiers（4 张 3:4 线稿占位卡）→ FAQ ×5（FAQPage JSON-LD）

### 4.3 攻略详情模板（mockups/performance.html，P0 样板）
页头 → Briefing → 分节正文（H2 白 / H3 cyan）→ 数据表（差异行 amber 高亮）→ mono 代码块（右上角 COPY 按钮）→ 折叠 FAQ → 补丁追踪时间线表

### 4.4 职业页（mockups/classes.html）
页头 → Briefing → **TierListBoard**（S-D 五行色带，tier-chip 56px 方格）→ 职业对比表（ClassTable：定位/招牌/难度星级/Tier pill）→ 分节深度内容 → Related：Builds / Bond 矩阵 / 单角色页

### 4.5 决策页（mockups/worth-it.html）
页头 → **ScoreBanner** 三格（Metacritic 大数字 success 色 / Steam 百分比 + 回升迷你柱图 / 结论格）→ Briefing → 三栏判定卡（✓ Buy / ⏸ Wait / ✕ Skip）→ danger 边框性能警告卡 → 版本对比表 → FAQ

### 4.6 其余页面（同模板复用）
- 角色单页 = 页头 + 立绘卡 + 技能表 + Bond 推荐 + 配音演员（链 voice-cast）
- `/weapons/` = WeaponDataTable（TanStack Table 可排序）
- `/trophy-guide/` = TrophyTable（53 成就，易错过 amber 标记）
- `/wiki/` = 字母索引条 + 紧凑词条列表（内链枢纽，轻设计）

---

## 5. 组件清单（17 个，shadcn 颗粒度）

SiteHeader / Breadcrumbs / HeroCinematic（融合版首屏）/ PageHero / HotTicker / FactGrid / HubNavCard / MissionBriefing / ArticleCard / CharacterCard / ClassTable / TierListBoard / WeaponDataTable / SpecCompareTable / ScoreBanner / FAQAccordion / SiteFooter

## 6. 实现备注（给 Codex）

1. 脚手架：Next.js + Tailwind + shadcn（"游戏站脚手架-main"），深色唯一主题，token 写入 CSS variables
2. 表格 = TanStack Table；图标 = lucide-react；Accordion/Dialog(CmdK) = shadcn 原语
3. 图片：next/image + WebP；Hero/角色立绘用占位渐变 + 线稿 SVG，路径集中在 `/public/assets/` 便于替换
4. 结构化数据：Article / FAQPage / VideoGame / AggregateRating 按页型注入
5. 移动端：表格横向滚动 + 首列冻结；字号 ≥16px；触控 ≥44px；Hero h1 降至 34px
6. OG 图模板 1200×630：金标题 + cyan HUD 边框 + 页型标签，Reddit 预览即广告
7. **P0 上线顺序**：performance → system-requirements → worth-it → classes/best → walkthrough 目录
